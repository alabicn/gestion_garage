{% extends 'base.html.twig' %}
{% block title %}{{title}}{% endblock %}
{% block body %}
<div class="container-sm">
    <div class="d-flex bd-highlight">
        <div class="p-2 w-100 bd-highlight">
        <h1>{{title}}</h1>
        </div>
        <div class="p-2 flex-shrink-1 bd-highlight"><a href="{{ path('garages') }}" class="btn btn-outline-primary" role="button"><i class="fas fa-arrow-left"></i></a></div>
    </div>
    {{ form(addGarageForm) }}

        <!--<div class="form-group">
            <p>Statut</p>
            <div class="custom-control custom-radio">
                <input type="radio" id="open" name="statut" class="custom-control-input" checked="">
                <label class="custom-control-label" for="open">Ouverte</label>
            </div>
            <div class="custom-control custom-radio">
                <input type="radio" id="close" name="statut" class="custom-control-input">
                <label class="custom-control-label" for="close">Fermée</label>
            </div>
        </div>-->
</div>

{% endblock %}